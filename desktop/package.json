{
  "name": "mygpt-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Native Windows shell for My_GPT 4 Students",
  "scripts": {
    "build:frontend": "cross-env BASE_PATH=./ PORT=5173 NODE_ENV=production pnpm --filter @workspace/student-ai run build",
    "build:backend": "pyinstaller --noconfirm --clean --distpath ./src-tauri/binaries --workpath ./build/pyinstaller mygpt-backend.spec",
    "build:tauri": "tauri build",
    "build:all": "pnpm run build:frontend && pnpm run build:backend && pnpm run build:tauri",
    "tauri": "tauri",
    "dev": "tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "cross-env": "^7.0.3"
  }
}
