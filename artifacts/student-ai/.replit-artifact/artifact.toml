kind = "web"
previewPath = "/"
title = "My_GPT 4 Students"
version = "1.0.0"
id = "artifacts/student-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24535

[services.development]
run = "pnpm --filter @workspace/student-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/student-ai", "run", "build" ]
publicDir = "artifacts/student-ai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24535"
BASE_PATH = "/"
